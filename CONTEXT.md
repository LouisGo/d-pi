# OMP 桌面会话客户端

让用户通过图形界面表达开发需求、查看 OMP 的执行过程和结果，并继续同一项工作。

## Language

**Thread（任务）**：用户在桌面应用中持续推进的一项工作；同一项目可有多个 Thread 并行。可直接在项目目录工作，也可在独立 Git worktree 工作；Thread 使用稳定 App ID，原生会话引用在确认后绑定，工作目录与进程代次独立；持久化与恢复遵守 [基础契约](docs/architecture/foundation-contracts.md)。

**任务工作目录**：Thread 实际读写项目文件的目录，可以是项目原目录或独立 Git worktree。多个 Thread 使用同一目录时共享文件状态；会话历史独立不等于文件修改隔离。

**OMP 会话**：由 OMP 管理的工作记录及其上下文，用户可以在其中追加需求，并在退出后恢复工作。
_Avoid_：GUI 消息列表（不能替代完整会话）

**会话镜像**：客户端依据 OMP 已提供的信息维护的展示状态，不是另一份执行事实或会话记录。
_Avoid_：客户端执行引擎

**草稿**：用户尚未提交的输入内容；不属于 OMP 已接受的会话输入。

**待回答交互**：OMP 发起、需要用户提供回答或确认后才能继续的交互。

**排队追加**：用户在 OMP 执行期间提交后续需求，等待 OMP 在后续处理时机消费；已提交的追加需求不再属于草稿。
执行期间普通发送默认采用此行为；干预与停止使用独立入口。

**干预**：用户在执行期间显式提交用于调整当前工作的要求；提交成功不表示要求已经被执行采纳。
_Avoid_：强制停止（不同操作）

**Side Chat（旁路问答）**：用户在主会话执行期间或完成后，针对选中文本或段落发起的独立问答；初始携带发起时的主会话上下文，可只读查看项目代码并继续追问，可独立切换模型与思考档位。关闭面板后保留历史，并关联主 Thread 与原文位置；追问沿用自身对话，用户可显式同步主会话最新进展。不打断主会话，不修改文件或自动追加主会话指令；用户可显式将结论发送到主会话。
_Avoid_：排队追加、干预（不能把旁路提问自动作为主会话的新要求）

**关闭窗口**：收起当前交互窗口，应用和正在进行的工作继续存在。
_Avoid_：退出应用

**退出应用**：结束整个桌面应用，并处理尚未完成的工作及其资源。

**Git Panel**：围绕 OMP 工作流呈现变更、检查结果并操作 Git 的应用面板。GUI 由本项目组织，底层 Git 机制复用成熟实现；不以完整通用 Git 客户端为目标。设计预留 Agent Changes / Run Changes / Review / Revert，具体 Run 边界、改动归属与 Revert 效果待相应阶段定义，不将这些名称直接等同 OMP 原生对象或 Git 命令。
_Avoid_：把全部工作区差异当成 Agent 改动；把 Git Panel 的 Revert 自动解释为会话回退。

**提交（Submission）**：一次用户发送的冻结内容与交接身份，区别于草稿和 OMP 执行；接收结果可能为未知，不能自动重发或凭文字去重。

**项目执行信任**：允许在指定项目启动 OMP 并加载原生项目配置/扩展；不是操作系统沙箱，也不证明 Agent 无法访问目录外资源。GUI 文件授权与 OMP 工具审批分工见[基础契约](docs/architecture/foundation-contracts.md)。
