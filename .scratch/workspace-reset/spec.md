---
title: 验证完成后的工作区清理
status: corrected
---

# 目标与范围

> 纠正：用户明确原意是整理与优化，不是清空既有选型、原型和可复用资产。下文“清理结果”描述 `a56ea59` 实际做过的删除，不代表当前认可其全部范围。此次按 `6fab3ef` 恢复完整归档，并重写当前入口；针对旧可运行应用与新方向的冲突，用户已选择保留完整旧基线、后续逐项复用。

按用户要求，在下一阶段需求讨论和技术选型前，移除提前建设的产品实现及约束，保留可行性证据和已确认的边界。清理前基线为 Git 提交 `6fab3ef`，原工作区干净。

## 清理结果

- 删除早期 `src/` 应用：单会话 Main/Host/preload、协议与解码器、React UI 和样式。没有将其重构成新的生产基建。
- 删除 package、锁文件、workspace、Electron Vite、TypeScript 和组件生成配置，解除提前选择的前端依赖与构建方案。
- 删除旧 `.scratch/omp-gui-m1/` 原型、上游源码副本和旧范围文档；Git 保留可追溯历史。
- 删除提前拟定的 V1 实现路线、前端库雷达及重复 handoff；同步移除 AGENTS 中的选型约束。
- 最新验证报告集中到 `docs/validation/`；旧 GUI 实测移至 `docs/archive/` 并明确历史属性。README 作为唯一当前阶段入口。
- 保留领域术语、两份已确认 ADR，以及独立 Runtime/Settings/随包探针和机器结果，避免重复验证。实验使用版本不等于后续前端技术选型。

## 检查与边界

提交前检查：所有保留实验脚本的 Node 语法、结果 JSON、Markdown 本地链接、删除项引用和 Git 空白错误；确认探针不依赖已删除的应用或工程配置。

本次不修改探针行为，不重复运行已通过的模型及随包实验；历史通过结果不代表当前存在产品应用。没有新增 UI、依赖或下一阶段实现。

## 后续纠正

- [完整归档](../../docs/archive/pre-reset/README.md)保存被删源码、测试、构建配置、M1 研究/原型/结果，以及当时全部 docs/prototype 内容；manifest 提供原路径、来源提交及 SHA-256。
- 恢复并整理 frontend-library-radar、v1-architecture-draft、handoff，保留自有组件层、Beautiful UI/Tool UI、内容/性能/通信/数据候选，以及原生行为与验证边界。
- 当前需求和选型文档明确继承关系：Biome、Streamdown、随包 Runtime、先只读后编辑取代旧方案对应条款，其他已有结论不无故清零。
- 用户已选择：根 src/package 等不直接恢复为当前可运行应用；完整保留归档，后续逐项复用协议解码、测试与生命周期经验。不运行旧实验或覆盖现有讨论修改。
