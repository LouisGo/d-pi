# 首个可运行里程碑：访谈记录

## 已确定

- 复用完整 OMP，投入 GUI 体验；utility process 是首版中间层起点。
- 单个本地项目、单个会话：编辑需求 → OMP 读写文件／执行命令 → 查看输出与工具结果 → 追加要求或停止 → 退出后恢复会话。用户已明确选择该验收闭环。
- 多会话并行和复杂分支后置。
- 沿用本机模型配置与登录，接住内置工具及 RPC 可表达的提问／确认；自定义 TUI 界面不在首版兼容承诺内。用户已明确选择。
- 用户确认以真实 Electron + 本机 OMP 开发闭环作为最终验收，用可控 RPC 数据补测异常；原型不能代替 GUI 验收。

## 收束结果

- `to-spec` 已安装并用于规格合成，规格见 [spec.md](spec.md)。不再继续产品访谈。
- 基础链路、确认交互、刷新重连、bash 取消、正常退出和原生会话恢复已做原型，见 [研究记录](research/findings.md)。
- 正式 GUI、真实模型、异常进程树清理与大帧处理尚未验收，纳入实现完成条件。

## Comments

- 2026-09-24：读取原讨论最近十轮，保留最终产品约束；早期通用 Workbench、跨框架分层和框架选型建议不视为用户已确定决策。本次未开展新的 GUI 项目广泛比较。
- 2026-09-24：用户中途要求暂停并安装 `to-spec`，安装完成后明确恢复；沿用此前答案，无重复确认产品方向。

- 2026-09-24：完整 V1 改以 `docs/prototype/v1-architecture-draft.md` 为目标；单会话是阶段 1 的交付边界，多会话与全 GUI 初始化在后续阶段兑现。原 M1 规格标记待修订，早先的后置项不再理解为 V1 排除项。

- 2026-09-24：用户确认关闭窗口继续工作、Dock 重开接回，真正退出按等待／停止／取消处理；阶段 1 执行中默认排队追加，提供独立干预入口。相关产品缺口已收束，OMP 具体执行语义仍需原型验证。
