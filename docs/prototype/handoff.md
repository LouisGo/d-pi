# 新窗口交接：从阶段 1 开始

项目：`/Users/lou/Learn/d-pi`。当前只有约定、架构、规格和接入原型，尚无正式应用。不要重新展开架构访谈，先完成阶段 1 的规格修订和必要原型，再按新任务授权进入实现。

**先读：**根目录 `AGENTS.md`、`CONTEXT.md`，以及 [V1 架构与阶段计划](v1-architecture-draft.md)。它定义完整 V1；[旧 M1 规格](../../.scratch/omp-gui-m1/spec.md)标记为 `needs-revision`，不能直接照旧开工。[研究记录](../../.scratch/omp-gui-m1/research/findings.md)区分实测与未验证事项。

**已定，不再反复确认：**复用完整 OMP；Main 管应用，utility SessionHost 管连接和同步，OMP 管执行，React 管体验。使用架构文档选定的技术栈。V1 按单会话可用、多会话、全 GUI 初始化、体验打磨、发布五阶段兑现。

**阶段 1：**已有 OMP 用户能在可见 GUI 完成真实开发。包含基本输入编辑、草稿、流式阅读、工具卡片、提问回答、停止、刷新重连和原生恢复；本地打包构建也要验证，不能只跑开发服务器。

**最新产品决定：**关闭窗口继续运行，从 Dock 重开接回；Cmd+Q／退出菜单才真正退出，有未完成工作时可等待完成、停止后退出或取消退出。执行中默认排队追加，另有明确的干预入口；不把干预偷换成强制停止，也不把接收确认显示成已生效。

**下一步：**针对目标 OMP 版本验证 `rpc-ui` 内置交互、原生排队／steering 的消费时机与顺序，以及停止对队列的影响；将结果和上述已定行为收进阶段 1 规格。一般实现选择自行决定，只有实质改变产品行为的缺口才追问。

**证据边界：**已有原型在 Electron 42.11.1、OMP 18.3.0 下通过 7 项检查，但使用隐藏窗口、本地固定模型响应、`rpc` 与测试扩展。未证明真实 GUI 体验、`rpc-ui` 内置交互、真实 Agent 取消、故障进程树清理或完整大帧同步；路径硬编码和测试清理逻辑不能直接当生产代码。

认证助手是提前验证支线，不阻塞单会话交付；启动该支线前再问首批供应商／认证方式。不要为了完整 V1 提前铺开所有工单、安装器或重型界面组件。阶段完成要给出实际运行证据与限制。
